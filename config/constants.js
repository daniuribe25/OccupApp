const quoteState = {
    SENT: 'Sent',
    ANSWERED: 'Answered',
    REJECTED: 'Rejected',
    ARCHIVED: 'Archived',
}

const pushActions = {
    SENT: 'Sent',
    ANSWERED: 'Answered',
}

module.exports = {
    quoteState,
    pushActions,
}